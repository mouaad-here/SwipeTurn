services:
  caddy:
    image: caddy:2-alpine
    container_name: swipeturn-proxy
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - swipeturn-caddy-data:/data
      - swipeturn-caddy-config:/config
    networks:
      - swipeturn-network

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: swipeturn-api
    restart: unless-stopped
    env_file:
      - ./backend/.env
    expose:
      - "8003"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8003/health"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - swipeturn-network
    volumes:
      - swipeturn-hf-cache:/models/hf
    environment:
      HF_HOME: /models/hf

  pipeline:
    build:
      context: ./pipeline
      dockerfile: Dockerfile
    container_name: swipeturn-pipeline
    restart: unless-stopped
    env_file:
      - ./pipeline/.env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - swipeturn-network
    volumes:
      - swipeturn-hf-cache:/models/hf
    environment:
      HF_HOME: /models/hf

networks:
  swipeturn-network:
    driver: bridge

volumes:
  swipeturn-caddy-data:
  swipeturn-caddy-config:
  swipeturn-hf-cache:
